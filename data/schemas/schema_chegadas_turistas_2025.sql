CREATE TABLE IF NOT EXISTS `tab_chegadas_turistas_2025` (
    `Via_de_acesso` VARCHAR(255) NULL,
    `UF` VARCHAR(255) NULL,
    `nome_pais_correto` VARCHAR(255) NULL,
    `mes` VARCHAR(255) NULL,
    `ano` INT NULL,
    `Chegadas` INT NULL
);