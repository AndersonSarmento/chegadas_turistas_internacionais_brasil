CREATE TABLE IF NOT EXISTS `tab_chegadas_turistas_2010` (
    `Continente` VARCHAR(255) NULL,
    `Ordem continente` INT NULL,
    `País` VARCHAR(255) NULL,
    `Ordem país` INT NULL,
    `UF` VARCHAR(255) NULL,
    `Ordem UF` INT NULL,
    `Via de acesso` VARCHAR(255) NULL,
    `Ordem via de acesso` INT NULL,
    `Ano` INT NULL,
    `Mês` VARCHAR(255) NULL,
    `Ordem mês` INT NULL,
    `Chegadas` INT NULL
);