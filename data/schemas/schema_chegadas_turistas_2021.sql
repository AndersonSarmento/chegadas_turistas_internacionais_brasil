CREATE TABLE IF NOT EXISTS `tab_chegadas_turistas_2021` (
    `Continente` VARCHAR(255) NULL,
    `cod continente` INT NULL,
    `País` VARCHAR(255) NULL,
    `cod pais` INT NULL,
    `UF` VARCHAR(255) NULL,
    `cod uf` INT NULL,
    `Via` VARCHAR(255) NULL,
    `cod via` INT NULL,
    `ano` INT NULL,
    `Mês` VARCHAR(255) NULL,
    `cod mes` INT NULL,
    `Chegadas` INT NULL
);