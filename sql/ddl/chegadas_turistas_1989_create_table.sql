CREATE TABLE IF NOT EXISTS `chegadas_turistas_1989` (
    `Continente` VARCHAR(255),
    `Ordem continente` INT,
    `País` VARCHAR(255),
    `Ordem país` INT,
    `UF` VARCHAR(255),
    `Ordem UF` INT,
    `Via de acesso` VARCHAR(255),
    `Ordem via de acesso` INT,
    `ano` INT,
    `Mês` VARCHAR(255),
    `Ordem mês` INT,
    `Chegadas` INT
);